package com.example.ccandroidtraining.adapter

import androidx.fragment.app.Fragment
import com.example.ccandroidtraining.activities.ViewPagerSlider.PlaceholderFragment.Companion.newInstance
import com.example.ccandroidtraining.fragments.PlaceholderFragments

class SliderAdapter{
//    fun getItem(position: Int): Fragment {
//        // getItem is called to instantiate the fragment for the given page.
//        // Return a PlaceholderFragment (defined as a static inner class below).
//
//        return PlaceholderFragments.newInstance(position + 1)
//    }
//
//    fun getCount(): Int {
//        // Show 5 total pages.(we will use 5 pages so change it to 5)
//        return 5
//    }

}